#ifndef __MAPHALLWAYSWALLS_H__
#define __MAPHALLWAYSWALLS_H__

// A basic function just to build the '#' around the hallway
void buildWalls(NormalRoom * room, char axis, int isFirst, int axisSwap, Tile ** map, int cols, int rows);

#endif